# Siddharth Tripathi — Portfolio

Minimal, responsive portfolio built with plain HTML, CSS and JavaScript.

## Included
- Hero / introduction
- About + education
- Selected projects
- Technical skills
- Achievements
- Resume section
- Contact links
- Dark/light mode
- Scroll progress indicator
- Scroll-reveal animations
- Mobile responsive layout
- Reduced-motion support

## Deploy on GitHub Pages

1. Create a new GitHub repository, for example `portfolio`.
2. Put all files from this folder in the repository root.
3. Commit and push:
   ```bash
   git add .
   git commit -m "Add portfolio website"
   git push
   ```
4. In GitHub: **Settings → Pages → Deploy from branch → main → / (root)**.
5. Save and open the generated Pages URL.

## Notes
The project cards currently link to the supplied GitHub profile rather than inventing individual repository URLs. Replace those links with the exact repository URLs once the corresponding repositories are confirmed.
